# Core dependencies
pillow
piexif
python-dotenv
pydantic
gradio

# OpenAI-related (if used inside MetadataAgent or Agent)
openai

# Async and typing are built-in to Python >=3.7
# CSV and os are built-in

# Minimum Python version
# (Add if your project requires a specific version)
python>=3.9
